<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appcolormemory">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppColorMemory">
        <activity android:name=".Connexion"></activity>
        <activity android:name=".Inscription" />
        <activity android:name=".NiveauChrono" />
        <activity android:name=".NiveauExpert" />
        <activity android:name=".NiveauDifficile" />
        <activity android:name=".DemarrageApp" />
        <activity android:name=".MainActivity" />
        <activity android:name=".RecommencerNiveau" />
        <activity android:name=".Profil" />
        <activity android:name=".MainMenu" />
        <activity android:name=".ChoixNiveau">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>